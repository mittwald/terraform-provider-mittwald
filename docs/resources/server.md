---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mittwald_server Resource - terraform-provider-mittwald"
subcategory: ""
description: |-
  This resource models a (virtual) server for a specific mittwald customer.
  Note: Ordering a server is a cost-intensive operation and will incur additional costs.
---

# mittwald_server (Resource)

This resource models a (virtual) server for a specific mittwald customer.

**Note:** Ordering a server is a cost-intensive operation and will incur additional costs.

## Example Usage

```terraform
data "mittwald_article" "server" {
  filter = {
    tags = ["vserver", "balance-optimized"]
    attributes = {
      "ram"  = "2"
      "vcpu" = "1"
    }
  }
}

resource "mittwald_server" "example" {
  customer_id = var.customer_id
  article_id  = data.mittwald_article.server.id

  description  = "My first server"
  diskspace_gb = 50

  use_free_trial = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `article_id` (String) The article ID determining the machine type of the server. This may be used to change the machine type at any time. When changing to a lower tier, the change will only become active after the contract duration (this may result in undefined behavior in the Terraform plan).
- `customer_id` (String) ID of the customer for which the server should be ordered
- `description` (String) A human-readable description for the server
- `diskspace_gb` (Number) The amount of disk space for the server, in GiB

### Optional

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `use_free_trial` (Boolean, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Use a free trial period for the server, when available. Only applicable on creation, not on updates.

### Read-Only

- `cluster_name` (String) The name of the cluster the server is running on
- `contract_id` (String) The contract ID associated with the server
- `created_at` (String) The time at which the server was created
- `id` (String) The ID of the server
- `machine_type` (String) The machine type of the server, as resolved from the article
- `short_id` (String) The short ID of the server (for example `s-4e7tz3`)
- `status` (String) The current status of the server
